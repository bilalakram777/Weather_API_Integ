Weather App (Python)
This is a simple command-line Weather App built in Python using the WeatherAPI.com service. It allows users to:

Enter a city name and their API key

Fetch real-time weather data (temperature and condition)

Display the current weather information in a user-friendly format

The script uses the requests library to make API calls and demonstrates error handling for common issues like invalid input or API errors.

